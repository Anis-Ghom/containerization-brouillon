# COMMIT: All 4 members (each add env vars your service uses; all 4 review).
# Your task: Keep in sync with docker-compose. Copy to .env and set values.

# Database password for API1 (Products). Default: api1secret
API1_DB_PASSWORD=api1secret

# Database password for API2 (Orders). Default: api2secret
API2_DB_PASSWORD=api2secret

# Optional: for CI/CD (Gitea secrets, not in .env)
# DOCKERHUB_USERNAME=your-dockerhub-username
# DOCKERHUB_TOKEN=your-dockerhub-token
